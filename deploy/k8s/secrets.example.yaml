# Template only. In production these values come from a vault (Azure Key Vault /
# KMS) via a CSI driver or workload identity — DO NOT commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: rr-secrets
  namespace: risk-register
type: Opaque
stringData:
  DATABASE_URL: "postgres://rr_app:REPLACE@db:5432/risk_register"
  ENTRA_TENANT_ID: "REPLACE"
  ENTRA_API_AUDIENCE: "api://risk-register"
  GRAPH_CLIENT_ID: "REPLACE"
  GRAPH_CLIENT_SECRET: "REPLACE"
  GRAPH_SENDER_UPN: "grc-notifications@yourtenant.onmicrosoft.com"
