{
  "name": "risk-register-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise Risk Register Platform — Biltema · Birgma. Containerized, secure-by-design GRC application.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "up": "docker compose up --build",
    "down": "docker compose down -v",
    "certs": "./deploy/scripts/gen-certs.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "//overrides": "Force a single React 19 across the tree — @testing-library/react otherwise auto-installs its own react/react-dom 18 peer, which mismatches the app's React 19 at reconcile time.",
  "overrides": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "license": "UNLICENSED"
}
